module.exports = {
  launch: {
    headless: false,
    slowMo: 70,
    devtools: true
  }
}

// headless: on => a browser does not actually launch to perform the test
// slowMo: off => no slow motion
